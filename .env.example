# Sprites.dev sandbox token (required for sandboxed execution)
# Get one at https://sprites.dev or run: sprite login && sprite org auth
# SPRITES_TOKEN=org/account-id/token-id/token-value

# Anthropic API key (required only if using :anthropic provider)
# ANTHROPIC_API_KEY=sk-ant-...

# Ollama endpoint (optional, defaults to http://localhost:11434)
# OLLAMA_URL=http://localhost:11434
